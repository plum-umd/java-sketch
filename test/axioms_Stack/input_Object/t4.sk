package t4;

// pop(pop(push(push(s, o1), o2))) == o1
harness void mn() {
  Object s = object();
  Object o1 = object();
  Object o2 = object();

  Object psh1 = push(s, o1);
  Object psh12 = push(psh1, o2);

  Object poppsh1 = pop(psh1);
  Object poppoppsh1 = pop(poppsh1);

  assert equals_Axiom(xform(poppoppsh1, ??), o1);
}
